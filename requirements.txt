# Multi-Agent Customer Support System - Dependencies
# Python 3.8+
#
# Notes:
# - This project intentionally uses the Python standard library for its core logic.
# - SQLite support comes from the standard library `sqlite3` module (do NOT add it here;
#   `pip install sqlite3` will fail because it is not a PyPI package).

# Optional: for running tests locally
pytest>=7.4.0
pytest-cov>=4.1.0

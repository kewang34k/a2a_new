#
# Multi-Agent Customer Support System - Dependencies
#
# Python 3.8+
#
# Note: This project uses only the Python standard library for runtime
# (including sqlite3). The dependencies below are for testing only.
#

pytest>=7.4.0
pytest-cov>=4.1.0
